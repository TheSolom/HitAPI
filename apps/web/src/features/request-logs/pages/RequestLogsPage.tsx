import { ComingSoon } from '@/components/states/ComingSoon';

export function RequestLogsPage() {
    return (
        <ComingSoon
            title="Request logs"
            description="Searchable request-level logs with filters, status codes and latency."
            phase="Phase 4"
        />
    );
}

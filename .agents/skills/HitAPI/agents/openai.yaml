interface:
  display_name: "HitAPI"
  short_description: "Repo-specific patterns and workflows for HitAPI"
  default_prompt: "Use the HitAPI repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true